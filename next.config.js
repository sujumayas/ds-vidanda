/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['example.com'], // Add any external domains you'll be loading images from
    },
  }
  
  module.exports = nextConfig